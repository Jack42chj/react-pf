import styled from "@emotion/styled";

export const Dim = styled.div`
  position: fixed;
  display: flex;
  align-items: center;
  width: 100vw;
  height: 100vh;
  background-color: rgba(0, 0, 0, 0.8);
  z-index: 999;
`;
